export default {
    SET_NEWS(state, response) {
      state.news = response
    },
    SET_JOBS(state, response) {
      state.jobs = response
    },
    SET_ASKS(state, response) {
      state.asks = response
    },
    SET_USER(state, response) {
      state.user = response
    },
    SET_ITEMS(state, response) {
      state.items = response
    },
}
